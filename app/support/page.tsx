import Link from 'next/link'

const faqs=[['How do I verify a RADVORA product?','Use the verification page with the product serial or QR-linked verification flow.'],['How do I register a product?','Sign in to your RADVORA account and use product registration. Registration links the product to your account history.'],['Where are RF-performance numbers shown?','Only approved claims linked to supporting evidence are published. If evidence is still pending, the site will say so rather than estimate a result.'],['How do I request support or warranty service?','Sign in to your account and create a support or warranty request so it stays associated with the relevant product record.']]
export default function SupportPage(){return <main className="page-wrap"><div className="shell"><section className="page-head"><span className="kicker">SUPPORT & FAQ</span><h1>Help built around your product record.</h1><p>Verification, registration, compatibility, support and warranty are connected so service history can remain traceable.</p></section><section className="admin-grid">{faqs.map(([q,a])=><article className="panel" key={q}><h2>{q}</h2><p>{a}</p></article>)}</section><div className="actions"><Link className="pill light" href="/login">Open customer account →</Link><Link className="pill ghost" href="/verify">Verify product</Link><Link className="pill ghost" href="/compatibility">Compatibility</Link></div></div></main>}
